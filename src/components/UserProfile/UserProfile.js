import React from 'react'
import UserProfileContent from './UserProfileContent'

export default function UserProfile({ userDataInfo }) {
  return (
      <UserProfileContent userDataInfo={userDataInfo} />
  )
  
}
